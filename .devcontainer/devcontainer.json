{
    "name": "Python Conda 环境",
    "image": "continuumio/miniconda3:latest",
    "mountWorkspaceGitRoot": false,
    "extraHosts": ["host.docker.internal:host-gateway"],
    "features": {
        "ghcr.io/devcontainers/features/git:1": {
            "version": "latest"
        }
    },
    "postCreateCommand": "git config --global --add safe.directory ${containerWorkspaceFolder} && conda run -n base pip install mysql-connector-python redis psycopg2-binary",
    "customizations": {
        "vscode": {
            "settings": {
                "git.terminalAuthentication": true
            }
        }
    }
}